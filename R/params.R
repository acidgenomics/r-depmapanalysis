#' @name params
#' @inherit AcidRoxygen::params return title
#' @keywords internal
#'
#' @param colData `logical(1)`.
#'   Include cell line metadata.
#' @param release `character(1)` or `NULL`.
#'   DepMap quarterly release ID (e.g. `"20Q3"`).
#'   If left `NULL`, defaults to latest supported release.
#' @param rowData `logical(1)`.
#'   Include gene annotations as genomic ranges.
NULL
